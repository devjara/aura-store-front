import { StockBadgePipe } from './stock-badge-pipe';

describe('StockBadgePipe', () => {
  it('create an instance', () => {
    const pipe = new StockBadgePipe();
    expect(pipe).toBeTruthy();
  });
});
